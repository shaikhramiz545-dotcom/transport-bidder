{
  "name": "tbidder-backend",
  "version": "1.0.0",
  "description": "Minimal backend for Flutter app",
  "main": "server.js",
  "scripts": {
    "start": "cd backend && node src/server.js",
    "start:legacy": "node server.js",
    "dev": "cd backend && node --watch src/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5"
  }
}
